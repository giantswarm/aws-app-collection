api_version: generators.giantswarm.io/v1
app_catalog: control-plane-catalog
app_destination_namespace: tempo
app_name: tempo
app_version: 0.1.2
kind: Konfigure
metadata:
  annotations:
    config.kubernetes.io/function: |-
      exec:
        path: /plugins/konfigure
  name: tempo
name: tempo
